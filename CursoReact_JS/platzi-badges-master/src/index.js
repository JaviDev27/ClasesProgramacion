import React from   'react';
import ReactDOM from 'react-dom';

const element = <h1> Hello, Platzi HOLAAA</h1>
const container = document.getElementById('app');

//ReactDOM.render(--que--, --donde--)

ReactDOM.render(element,container);
